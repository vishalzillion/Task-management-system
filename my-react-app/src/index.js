import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // You should have an App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
